---
id: 05-01
wave: 1
autonomous: true
---

# Plan 05-01: ML Feature Engineering Extractor

## Objective
Implement `hackersec/analysis/ml/features.py` constructing robust matrix mappings representing isolated static arrays into bound `numpy` features securely digesting anomalous schema artifacts.

## Implementation Steps

### 1. `extract_features` Function
- Initialize `def extract_features(finding: Finding) -> list[float]`:
  - `static_confidence`: Map string `severity` definitions uniformly (e.g., `"CRITICAL" -> 1.0`, `"HIGH" -> 0.8`, `"MEDIUM" -> 0.5`, `"LOW" -> 0.2`).
  - `llm_confidence`: Isolate float parameter recursively. Default to `0.0` securely on dictionary lookup failures.
  - `cpg_taint_depth`: Length of nested `finding.cpg_context.get("taint_paths")` arrays limiting recursively.
  - `cwe_severity_score`: Map defaults based on hardcoded baseline dictionaries. Wait, rule length or standard bounds generally default to `0.5` missing specifics.
- Return lists structured precisely in order: `[static_confidence, llm_confidence, cpg_taint_depth, cwe_severity_score]`.

## Verification
- Unit bounds ensuring dict malformations strictly output safe zeroes mapped inside identical indices without failing structural lists.
