---
id: 05-04
wave: 2
autonomous: true
---

# Plan 05-04: Unit Testing & Boundary Validations

## Objective
Establish `pytest` wrappers locally simulating classifier endpoints preventing CI crashes explicitly verifying prediction constants map to accurate labels.

## Implementation Steps

### 1. `tests/test_ml.py`
- Formulate arrays covering baseline `Finding` dataclasses simulating `CRITICAL` rules with LLM confidences `= 0.99`.
- Evaluate `extract_features(finding)` asserting correct vector lists (e.g., `[1.0, 0.99, 1, 0.5]`).
- Validate the mock training pipeline generating synthetic matrices and verify `SHAP` boundaries explicitly test constraints preventing executions against simulated `false_positives`.

### 2. Integration Additions
- Append training hook dependencies natively into `Makefile` bridging `train: python hackersec/analysis/ml/train.py`.

## Verification
- Run `make test` asserting the data science limits adhere securely limiting latency boundaries underneath < 3 seconds total.
