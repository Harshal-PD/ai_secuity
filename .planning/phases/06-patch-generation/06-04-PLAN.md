---
id: 06-04
wave: 2
autonomous: true
---

# Plan 06-04: Celery Orchestration & Models

## Objective
Embed the patch execution boundary inside `hackersec/worker/tasks.py` mapping execution strings specifically targeting `true_positive` verdicts. Update the storage schema accurately preserving the validation constraints.

## Implementation Steps

### 1. `hackersec/worker/tasks.py` Updates
- Append Step 3.10: Patch Generation.
- Validate `finding.fusion_verdict == "true_positive"`.
- Execute `build_patch_prompt()` -> `llm_client.generate()` -> `parse_patch()`.
- Run `compute_diff()` capturing isolated array modifications.
- Execute `validate_patch()` enforcing constraint `fixed_status`.
- Assign `finding.patch = diff_string` and `finding.patch_status = fixed_status`.

### 2. Schema Adjustments
- Extend `hackersec/db/store.py` adding `patch_status = Column(String(50), nullable=True)` mapped to serialization bounds protecting the database queue mapping.

## Verification
- Invoke End-to-end `run_analysis` testing checking valid schema persistence and bypassing iterations safely for `false_positive` outputs.
