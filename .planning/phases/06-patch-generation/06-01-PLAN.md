---
id: 06-01
wave: 1
autonomous: true
---

# Plan 06-01: Patch Generation Prompter

## Objective
Establish `hackersec/analysis/patch/prompter.py` building LLM formatting blocks demanding exact source substitute code mapping existing line boundaries tightly without generalized Markdown padding.

## Implementation Steps

### 1. `build_patch_prompt` Mappings
- Write `build_patch_prompt(finding: Finding)` fetching `finding.code_snippet` and `finding.llm_analysis`.
- Direct the instruction payload: `"Re-write this vulnerable codebase securely fixing the root cause mapping exact syntactical structures identical to previous indentations without explanatory conversations."`
- Hook `[UNTRUSTED_CODE_START]` logic securely mitigating conversational prompts injected inside standard arrays.

### 2. `parse_patch` Wrappers
- Write `parse_patch(raw_text: str)` stripping markdown ` ```python ` barriers natively mapping identical execution layers cleanly removing Ollama conversational hallucinations (`Here and sure, ...`).

## Verification
- Unit string assertions simulating DeepSeek outputs confirming only raw python substitutions merge back.
