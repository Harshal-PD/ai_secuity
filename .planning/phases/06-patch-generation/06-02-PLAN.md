---
id: 06-02
wave: 1
autonomous: true
---

# Plan 06-02: Diff Computation Logic

## Objective
Establish `hackersec/analysis/patch/differ.py` manipulating python's `difflib` mapping raw modifications explicitly defining execution paths highlighting `+` additions seamlessly against baseline snippets.

## Implementation Steps

### 1. `unified_diff` Implementation
- Define `compute_diff(original: str, patched: str) -> str`.
- Ensure string arrays break around `\n` mappings securely converting outputs via `difflib.unified_diff(original.splitlines(), patched.splitlines(), lineterm='')`.
- Return concatenated lists formatting cleanly mapped diff mappings dynamically masking irrelevant surrounding code blocks limiting context depth securely without truncating the specific modifications.

## Verification
- Invoke `pytest` mock arrays verifying string formats identically emulate Git standard behaviors preventing unstructured visual strings.
