---
id: 07-04
wave: 2
autonomous: true
---

# Plan 07-04: Unit Testing & CLI Injection

## Objective
Embed mock matrices validating metric calculation bounds and map `Makefile` instructions triggering full benchmarking runs interactively.

## Implementation Steps

### 1. Verification Logic tests
- Establish `tests/test_eval.py` calculating mocked precision arrays proving F1 returns `< 1.0` dynamically mapped to zero safely avoiding interpreter crashes.

### 2. Makefile Hooks
- Register `make eval` binding `python hackersec/evaluation/runner.py` directly simulating outputs locally without triggering external queue infrastructure loops ensuring CI validations run instantaneously.

## Verification
- Invoke `pytest tests/test_eval.py` seamlessly evaluating arithmetic paths.
