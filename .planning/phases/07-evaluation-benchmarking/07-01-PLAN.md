---
id: 07-01
wave: 1
autonomous: true
---

# Plan 07-01: Dataset Preparation

## Objective
Establish `hackersec/evaluation/dataset.py` mapping simulation data arrays avoiding massive gigabyte network bottlenecks generating literal vulnerable/safe tests explicitly targeting Semgrep testing boundaries matching Juliet parameters safely.

## Implementation Steps

### 1. `dataset.py` Generator
- Write `generate_test_suite()` logic instantiating `.py` code blocks strictly masking known vulnerabilities locally inside `data/eval_set/`.
- Export `metadata.json` dict mapping `file_path => { "label": 1 or 0, "cwe": "CWE-..." }` defining pure truth bounds effectively checking native accuracy mapping false positives recursively.
- Expose `load_dataset()` dynamically mapping local configuration variables.

## Verification
- Validate the directories initialize smoothly mapping isolated files natively preserving data logic cleanly without timeouts.
