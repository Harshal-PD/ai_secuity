---
id: 07-02
wave: 1
autonomous: true
---

# Plan 07-02: Evaluation Harness

## Objective
Establish `hackersec/evaluation/runner.py` triggering the native HackerSec pipeline routines sequentially over the mock datasets calculating True Positive classifications seamlessly.

## Implementation Steps

### 1. Harness Execution
- Write `evaluate_pipeline()` executing internal variables bypassing actual Celery worker architectures forcing synchronous loops directly.
- Truncate output limits mapping array comparisons:
  - Baseline Label: Semgrep array length > 0 (`1`) vs metadata label.
  - HackerSec Label: Pipeline Output `fusion_verdict` equals `"true_positive"` (`1`) vs metadata label.

### 2. Matrix Tracking
- Ensure dictionaries natively capture False Positive and False Negative metrics cleanly mapping outputs correctly avoiding null calculations natively.

## Verification
- Invoke end-to-end strings mocking execution cleanly separating Semgrep bounds isolating accurate classification reports safely.
