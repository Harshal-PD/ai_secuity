---
id: 07-03
wave: 2
autonomous: true
---

# Plan 07-03: Metrics Aggregator & Exporter

## Objective
Establish `hackersec/evaluation/metrics.py` dynamically assessing Precision, Recall, F1, and FPR recursively exporting standardized JSON summaries mapping output trajectories accurately.

## Implementation Steps

### 1. Mathematical Formulas
- Write `calculate_metrics(results_array: list) -> dict` securely bypassing `ZeroDivisionError` mappings if Precision/Recall evaluate to empty.
- Format comparisons returning specific differences against `baseline_f1` and `hackersec_f1`.

### 2. Export Structures
- Dump output variables recursively generating directories dynamically saving files using datetime conventions natively (`eval_results/YYYY-MM-DD_run.json`).

## Verification
- String array configurations matching mathematical precision safely.
