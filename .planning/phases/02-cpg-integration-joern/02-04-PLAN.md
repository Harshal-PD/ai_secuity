---
id: 02-04
wave: 2
autonomous: true
---

# Plan 02-04: Testing & Mock Server

## Objective
Write integration tests ensuring the new CPG augmentation acts robustly towards pipeline exceptions, and ensure Makefile accommodates testing modes cleanly without a mandatory local Joern server.

## Implementation Steps

### 1. `tests/test_joern_client.py`
- Patch `httpx.post` inside `JoernClient` to mock server responses (success vs 500 error).
- Assert the Graceful parsing works accurately.

### 2. `tests/test_phase2.py`
- Validate that pipeline task skips CPG augmentation when target server raises a connection error.
- Assure database serialization outputs JSON cleanly within `/api/results` endpoint.

### 3. Updates to `Makefile`
- Standardize `start-joern: joern --server --port 9000` rule to streamline local development for pipeline phases.

## Verification
- Running `make test` triggers `pytest` covering all new tests securely without an active JVM instance locally.
