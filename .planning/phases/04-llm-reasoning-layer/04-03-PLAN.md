---
id: 04-03
wave: 2
autonomous: true
---

# Plan 04-03: Celery Pipeline Dispatch Integration

## Objective
Embed the finalized LLM client abstractions natively into `hackersec/worker/tasks.py` underneath RAG integration (Step 3.8). Secure timeouts preventing blocking threads across large repository arrays.

## Implementation Steps

### 1. `hackersec/worker/tasks.py` 
- Beneath Step 3.7 RAG Enrichment, designate Step 3.8: LLM Reasoning.
- Ensure instantiation of `OllamaClient` operates with aggressive `timeout=45.0` intervals.
- Loop over deduped variables pushing contextual boundaries inside `build_analysis_prompt`.
- Invoke `client.generate()` extracting parsing returns.
- Store resulting dictionaries under the database context placeholder `finding.fusion_verdict`. Wait, no, `fusion_verdict` is for Phase 5 (Scikit ML Fusion). It should map back against a specific `finding.llm_analysis` placeholder.

### 2. Schema Adjustments
- Extend `hackersec/db/store.py` adding `llm_analysis = Column(Text, nullable=True)` to map LLM validations downstream identically to CPG attributes.
- Ensure serialized updates parse back into FastAPI requests dynamically.

## Verification
- Validate celery loops gracefully tag `{ "llm_status": "failed" }` instead of failing the job UUID when Ollama rests unreachable.
