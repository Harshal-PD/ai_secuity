---
id: 08-03
wave: 2
autonomous: true
---

# Plan 08-03: Dashboard Logic & Components 

## Objective
Establish `App.jsx` coordinating component arrays defining Uploaders, Grids, and Modal Views capturing `finding` logic mapping pipeline responses.

## Implementation Steps

### 1. File Uploader Form
- Create `Upload.jsx` executing `formData` mappings POSTing to `http://localhost:8000/analyze`.
- Formulate interactive state mapping `job_id` correctly returning to a `Polling` phase looping `GET /jobs/{job_id}` seamlessly tracking `status`.

### 2. Finding Matrix UI
- Build `Card.jsx` visualizing rule parameters elegantly leveraging severity badges.
- Expand interactive properties triggering `Modal.jsx` displaying SHAP logic features and Raw text diff (`finding.patch`) isolating visualizations elegantly using unified diff syntax highlighting formats locally.

## Verification
- Invoke specific unit states manually proving JSON parses render aesthetically.
